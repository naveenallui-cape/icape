// i-CAPE Prisma schema
// Business models will be added in later phases.
// Core concept: Olympiad Year (e.g. 2026-2027) — NOT academic year.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
